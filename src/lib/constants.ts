export enum Roles {
  Adopter = 'Adopter',
  Shelter = 'Shelter',
  Admin = 'Admin',
}

export enum TypeOfResults {
  Animal = 'results',
  Organization = 'organizations',
}

export enum Variant {
  Animal = 'pet',
  Organization = 'organization',
}
